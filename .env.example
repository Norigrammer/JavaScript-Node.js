########################################
# Local Development Environment Example #
########################################
# Copy this file to `.env` and set real values for your machine.

# Node runtime
NODE_ENV=development
PORT=3000

# App session
SESSION_SECRET=change_me_in_production

# Feature flags
# Use mock data (no DB required) for screens preview in development
USE_MOCK_DATA=false

# Database (local dev)
# Uncomment and fill when you actually connect to MySQL locally
# DB_HOST=localhost
# DB_USER=your_local_db_user
# DB_PASSWORD=your_local_db_password
# DB_NAME=your_local_db_name

# GCP (production on App Engine)
# Set only in production runtime environment
# INSTANCE_CONNECTION_NAME=project:region:instance