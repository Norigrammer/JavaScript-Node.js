# Copy this file to `.env` and set real values for local development.# Copy to .env and fill values for local development

# Do NOT commit your real .env.DB_USER=your_mysql_user

DB_PASSWORD=your_mysql_password

# ApplicationDB_NAME=your_database_name

NODE_ENV=development# For Cloud SQL Unix socket only (production)

PORT=3000INSTANCE_CONNECTION_NAME=project:region:instance

SESSION_SECRET=change_me_in_production# Optional port override

PORT=3000
# Database (local dev)
DB_USER=your_local_db_user
DB_PASSWORD=your_local_db_password
DB_NAME=your_local_db_name
# DB_HOST is optional because app.js uses 'localhost' by default for dev
# DB_HOST=localhost

# GCP (production on App Engine)
# INSTANCE_CONNECTION_NAME=project:region:instance